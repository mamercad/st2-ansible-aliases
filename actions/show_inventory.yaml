---
name: show_inventory
pack: st2-ansible-aliases
description: Show server Ansible inventory /etc/ansible/hosts
runner_type: local-shell-cmd
enabled: true
entry_point: ""
parameters:
  kwarg_op:
    immutable: true
  cmd:
    immutable: true
    type: string
    description: "Command to run"
    default: "cat /etc/ansible/hosts"
